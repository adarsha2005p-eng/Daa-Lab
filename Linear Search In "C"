#include <stdio.h>

int main()
{
    int n, key, pos = -1;
    scanf("%d", &n);

    int a[n];
    for (int i = 0; i < n; i++)
        scanf("%d", &a[i]);

    scanf("%d", &key);

    for (int i = 0; i < n; i++)
    {
        if (a[i] == key)
        {
            pos = i;
            break;
        }
    }

    if (pos != -1)
        printf("Found at index %d", pos);
    else
        printf("Not found");

    return 0;
}
